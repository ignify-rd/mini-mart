/** Brand HSL for xtreme-ui (--H / --S / --L). Soft teal — dễ nhìn, không đỏ gắt. */
export const DEFAULT_THEME_COLOR = {
	h: 172,
	s: 38,
	l: 40,
} as const;

/** Approximate hex for PWA / meta theme-color */
export const DEFAULT_THEME_HEX = "#3f8f7e";
